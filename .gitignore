# 依赖与构建产物
node_modules/
dist/
build/
*.log

# 部署包（由 build-full-package.py 生成，不进仓库，可本地重新打包）
deploy/*.zip

# 运行时数据（用数据卷持久化，不进仓库）
uploads/
backups/
ehs_pg_data/

# 密钥与本地配置
.env
.env.*
!.env.example

# 系统文件
.DS_Store
Thumbs.db

# 本地 agent 工作记忆（非源码，不进备份）
.workbuddy/
